<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_sort_importance"
        android:orderInCategory="100"
        android:title="按优先级排序"
        app:showAsAction="never"
        tools:ignore="HardcodedText" />
    <item
        android:id="@+id/action_sort_end_time"
        android:orderInCategory="100"
        android:title="按结束时间排序"
        app:showAsAction="never"
        tools:ignore="HardcodedText" />
    <item
        android:id="@+id/action_sort_start_time"
        android:orderInCategory="100"
        android:title="按开始时间排序"
        app:showAsAction="never"
        tools:ignore="HardcodedText" />
    <item
        android:id="@+id/action_sort_name"
        android:orderInCategory="100"
        android:title="按名称排序"
        app:showAsAction="never"
        tools:ignore="HardcodedText" />
    <item
        android:id="@+id/action_show_completed"
        android:orderInCategory="100"
        android:title="显示已完成"
        app:showAsAction="never"
        tools:ignore="HardcodedText" />
</menu>
